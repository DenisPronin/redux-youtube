/*
 * Constants
 * */

/*
 * Actions
 * */
export const actions = {};

/*
 * State
 * */
const initialState = [];

/*
 * Reducers
 * */
export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
}
